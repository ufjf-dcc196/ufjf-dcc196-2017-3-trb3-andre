<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.andre.trb3">

    <uses-sdk tools:overrideLibrary="org.sqlite.database" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.CadastrarTarefa" />
        <activity android:name=".activitys.Tags" />
        <activity android:name=".activitys.CadastrarTag" />
        <activity android:name=".activitys.AtribuirTags" />
        <activity android:name=".activitys.TarefasAssociadas"></activity>
    </application>

</manifest>